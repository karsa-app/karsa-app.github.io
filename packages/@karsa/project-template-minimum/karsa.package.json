{
  "name": "@karsa/project-template-minimum",
  "kind": "template",
  "version": "0.1.0",
  "description": "Base template — chat, spec, exec, file, engine harness, and system subscribers. Every Karsa project starts from this.",
  "provides": {
    "artifact_kinds": ["chat", "spec", "exec", "file", "agent-profile", "subscriber", "event-handler", "syslog"]
  },
  "requires": {
    "karsa": ">=0.1.0"
  },
  "install": {
    "dest": "$KARSA_HOME/packages/@karsa/project-template-minimum",
    "archive": "tar.gz"
  },
  "upgrade": {
    "policy": {
      "index.md": "keep",
      "README.md": "keep",
      "schemas/**": "replace",
      "src/**": "merge",
      "*": "merge"
    },
    "note": "User-facing project home (index.md) is kept on upgrade; JSON schemas are replaced (upstream-owned); seed content is three-way merged against the shipped baseline (spec_01KVGG48KT08)."
  }
}
